# 에피소드 메모리: virtual-office-hangang-penthouse-layout

## 기본 정보

- Date: 2026-05-22
- Agent: Joi, TARS
- Project: Jarvis
- Task ID: `virtual-office-hangang-penthouse-layout`

## 회고

- 사용자는 C3PO 등 에이전트 캐릭터가 제공 팝업에 가려지는 문제를 보고했고, 배경도 더 강한 "최고층 한강뷰 집무실" 분위기로 바꾸길 원했습니다.
- 배경은 특정 IP 복제가 아니라 한강 파노라마, 최고층 펜트하우스, 미래형 테크 집무실의 조합으로 생성하는 것이 적절했습니다.
- 처음에는 선택된 요청이 다른 라이브 이벤트로 자동 전환되어 겹침 측정이 잘못 잡혔습니다. 이후 요청 선택값을 `virtual-office-hangang-penthouse-layout`으로 고정한 뒤 검증했습니다.
- C 드라이브 여유 공간이 한때 0바이트로 보고되어 스크린샷 저장이 불안정했습니다. 먼저 브라우저 DOM 좌표로 충돌 여부를 측정했고, 이후 여유 공간이 회복되어 스크린샷도 증거로 남겼습니다.

## 학습

- Virtual Office의 캐릭터 배치 검증은 최신 요청 자동 선택에 의존하지 말고, 검증 대상 requestId를 명시적으로 선택해야 합니다.
- 팝업형 제공 영역이 상단에 있을 때는 에이전트를 하단 안전 레인으로 배치하고 `agentJitter`를 0으로 고정하면 예측 가능한 레이아웃을 얻을 수 있습니다.
- 배경 생성 요청에 유명 IP 표현이 들어오면 스타일적 의도만 추출하고 고유 캐릭터, 로고, 세트 복제는 피해야 합니다.

## 기억 정책

- 보존: `virtual-office-hangang-penthouse-layout`의 안전 레인 좌표, 배경 자산 경로, 브라우저 좌표 검증 방식.
- 요약 가능: 생성 이미지 프롬프트 세부 표현.
- 민감정보: 없음.
